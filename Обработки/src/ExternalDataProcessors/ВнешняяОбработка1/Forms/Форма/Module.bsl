
&НаКлиенте
Процедура ЗапускДлительнойОперации(Команда)
	ФормаСообщения = ОткрытьФорму("ВнешняяОбработка.ВнешняяОбработка1.Форма.ФормаСообщений",,ЭтаФорма,,,,,РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	ЗапускДлительнойОперацииНаСервере();
	ФормаСообщения.Выполнено();
	Сообщить("Выполнено");
КонецПроцедуры

&НаСервере
Процедура ЗапускДлительнойОперацииНаСервере()
	
	Для к = 0 По 1000000 Цикл
		А = к;
	КонецЦикла;
	
КонецПроцедуры
